#!/usr/bin/env python3
"""Calculate volume from global dimension variables (all in metres)."""

# Global dimension variables (metres)
LENGTH = 5.0
WIDTH = 3.0
DEPTH = 2.0

# Calculate volume
volume = LENGTH * WIDTH * DEPTH

print(f"Volume: {volume} m³")
